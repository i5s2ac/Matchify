/* App.css */

/* Reset básico */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f9;
    color: #333;
    line-height: 1.6;
}

/* Estilos principales para App.js */

/* Contenedor principal que ajusta el contenido si la navbar lateral está visible o colapsada */
.app-container {
    min-height: 100vh;
    transition: margin-left 0.3s ease;
}

/* Ajuste de margen cuando la navbar está visible y expandida */
.main-content-expanded {
    margin-left: 16rem; /* Ancho de la navbar expandida */
    padding: 0rem;
    transition: margin-left 0.3s ease;
}

/* Ajuste de margen cuando la navbar está colapsada */
.main-content-collapsed {
    margin-left: 4rem; /* Ancho de la navbar colapsada */
    padding: 2rem;
    transition: margin-left 0.3s ease;
}
